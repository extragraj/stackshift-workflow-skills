# Architecture & Data Flow

<!-- Describe the system's data flow from trigger to render. Cover:
  - How the feature is triggered (user action, document reference, etc.)
  - What context or state manages the active instance
  - How variants are dispatched (registry map, switch, etc.)
  - Where variant components live in the file tree
-->

## <!-- System name --> shape

```typescript
// <!-- Paste the core TypeScript type or registry shape here -->
```
