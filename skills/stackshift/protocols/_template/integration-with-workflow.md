# Integration with the Base Workflow

A new variant using this protocol still follows Steps 1–5 of the base workflow,
with the additions and overrides documented here.

## Step 2 — Section Schema

<!-- Describe any schema-level differences from the standard rootSchema() setup.
     If no changes, write "No changes — follow the standard Step 2 workflow." -->

## Step 4 — Component Variant

<!-- List each sub-step (4a–4d) and call out only where this protocol diverges.
     If no changes to a sub-step, omit it. -->

**4a — Create the variant file**
<!-- Any differences from creating a standard empty variant file? -->

**4b — Register the variant**
<!-- Does the variant register in the standard Variants map, or elsewhere (e.g. a protocol-specific registry)? -->

**4c — Props interface**
<!-- Is the props interface per-variant, shared, or extended from a base type? -->

**4d — Implement via ui-forge**
<!-- Any constraints on what ui-forge should or should not generate? -->

## <!-- Any other affected steps -->

<!-- Document overrides for Steps 1, 3, or 5 only if this protocol affects them. -->
