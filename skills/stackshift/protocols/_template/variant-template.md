# Variant Template

<!-- Explain any constraints on how variants must be structured for this protocol
     (e.g. required primitives, accessibility rules, prop contract). -->

```tsx
// <!-- Paste the minimum boilerplate for a new variant here.
//      Replace placeholder names and fill in required imports. -->
```

<!-- Note any props, hooks, or wrapper components that every variant must use. -->
