{
  "name": "@extragraj/stackshift-skills",
  "version": "0.1.1",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "cli"
  ],
  "bin": {
    "stackshift": "./bin/cli.mjs"
  },
  "files": [
    "bin",
    "cli/dist",
    "skills",
    "skill.version"
  ],
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3"
  },
  "scripts": {
    "build": "npm --prefix cli run build",
    "prepack": "npm --prefix cli run build"
  }
}
