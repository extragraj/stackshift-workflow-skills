{
  "name": "@extragraj/stackshift-skills",
  "version": "0.2.0",
  "packageManager": "pnpm@9.0.0",
  "bin": {
    "stackshift": "./bin/cli.mjs"
  },
  "files": [
    "bin",
    "cli/dist",
    "skills",
    "skill.version"
  ],
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3"
  },
  "scripts": {
    "build": "pnpm --filter stackshift-cli build",
    "sync-version": "node scripts/sync-version.mjs",
    "prepack": "pnpm sync-version && pnpm build"
  }
}
